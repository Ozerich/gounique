$(document).ready(function () {

   /* $('#createstart-page #v_num, #createformular-page .vnum-input').setValidator({max_length:8});
    $('#createstart-page #r_num').setValidator({ high_letters:false, low_letters:false});

    $('.only-digits').setValidator({only_digits:true});

    $('#price-page input').setValidator({only_digits:true});
    $('.bonus-content input').setValidator({only_digits:true});
    $('.reservierung-page').find('.flight-price, .person-count, .servicecharge, .servicecharge-percent').setValidator({only_digits:true});
    $('.reservierung-page #intro-page input').setValidator({only_digits: true});
    $('#final-page .anzahlung-block input').setValidator({only_digits: true});
*/
});